[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "project_brain_decoder"
version = "0.1.0"
description = "Neural decoding of finger velocity from BCI recordings"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]